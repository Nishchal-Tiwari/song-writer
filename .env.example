# Backend (production default; use 3000 for local dev if preferred)
PORT=3005
DATABASE_URL=postgres://phonetics:phonetics@localhost:5433/phonetics
PHONETIK_URL=http://127.0.0.1:1273
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o
GENERATION_BATCH_SIZE=30
GENERATION_MAX_RETRIES=3

# Frontend (optional — not needed in production; API is same-origin at /api)
VITE_API_URL=http://localhost:3005
